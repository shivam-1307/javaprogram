Clearall

disp("enter no of observation")

n = input()

disp("value of p")

p-input(")

fori=1:n

X(1, i) = underline input (^ prime ) end

disp("enter the vlaue of x") disp("enter no of frequency") F(1, j) = underline input (?^ prime prime ) end

forj=1:n

EF=sum(F)*binomial(p.n-1)

disp("Given frequencies")

disp(F)

disp("Expected frequencies")

disp(EF)

plot2d3(0:n-1,F)

plot2d(0:n-1,EF)
